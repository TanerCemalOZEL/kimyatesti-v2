@model IEnumerable<kimyatesti.Models.SoruHistory>

@{
//Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ogrenci.OgrName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soru.ImgPath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TestGroup.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YBD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OgrencininCevabi)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ogrenci.OgrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Soru.ImgPath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TestGroup.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.YBD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OgrencininCevabi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
