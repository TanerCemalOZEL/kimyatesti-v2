@model IEnumerable<kimyatesti.Models.OgrenciTestGroupBind>

@{
//Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Öğrenciyi test grubuna bağla", "Create", null, new { @class="btn btn-success btn-sm"})
    </p>
    <table class="table table-striped table-hover">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ogrenci.OgrName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TestGroups.Name)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ogrenci.OgrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TestGroups.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
