@model IEnumerable<kimyatesti.Models.OgrenciAvatar>

@{
//Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OgrName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OgrEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kurum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MevcutSinif)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OgrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OgrEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kurum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MevcutSinif)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
